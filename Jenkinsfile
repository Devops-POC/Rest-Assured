node {
    stage('build'){
        echo "building"
    }
}
stage('Deploy approval'){
    //input "Deploy to prod?"
    echo "approval"
    // commit
}
node {
    stage('deploy to prod'){
        echo "deploying"
    }
}
